public < E extens Compareable<E> > E maximum (E a, E b, E c) {
	E max = a; // antar at max verdien er a.

	if (b.compareTo(a) > 0) 
		max = b;
	if (c.compareTo(max) > 0)
		max = c;
	return max;
}
